import * as _ from 'lodash';
import * as express from 'express';

export const utils = {
    consoleLog(message: string) {
        /* tslint:disable */
        console.log(message);
        /* tslint:enable */
    },
};
